import React, { Component } from 'react';
import Button from 'muicss/lib/react/button';

class ToolBox extends Component {
  render() {
    return (
      <div>
        <Button
          color="primary"
          onClick={() => this.props.onPropChange({ brushCol: '#000000' })}
        >
          Black
        </Button>
        <Button
          color="primary"
          onClick={() => this.props.onPropChange({ brushCol: '#000000' })}
        >
          Black
        </Button>
        <Button
          color="primary"
          onClick={() => this.props.onPropChange({ brushCol: '#000000' })}
        >
          Black
        </Button>
        <Button
          color="primary"
          onClick={() => this.props.onPropChange({ brushCol: '#000000' })}
        >
          Black
        </Button>
        <Button
          color="primary"
          onClick={() => this.props.onPropChange({ brushCol: '#000000' })}
        >
          Black
        </Button>
        <Button
          color="primary"
          onClick={() => this.props.onPropChange({ brushCol: '#000000' })}
        >
          Black
        </Button>
        <Button
          color="primary"
          onClick={() => this.props.onPropChange({ brushCol: '#000000' })}
        >
          Black
        </Button>
        <Button
          color="primary"
          onClick={() => this.props.onPropChange({ brushCol: '#000000' })}
        >
          Black
        </Button>
        <Button
          color="primary"
          onClick={() => this.props.onPropChange({ brushCol: '#000000' })}
        >
          Black
        </Button>
        <Button
          color="primary"
          onClick={() => this.props.onPropChange({ brushCol: '#000000' })}
        >
          Black
        </Button>
        <Button
          color="primary"
          onClick={() => this.props.onPropChange({ brushCol: '#000000' })}
        >
          Black
        </Button>
      </div>
    );
  }
}
export default ToolBox;
